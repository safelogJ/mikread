<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- При нажатии -->
    <item android:state_pressed="true">
        <layer-list>
            <!-- Фон (ваш shape) -->
            <item>
                <shape>
                    <solid android:color="@color/winbox_btn_down"/>
                    <stroke android:width="1.7dp" android:color="@color/winbox_btn_border_down"/>
                    <corners android:radius="0.5dp"/>
                </shape>
            </item>
            <!-- Иконка (центрирована) -->
            <item android:drawable="@drawable/red_remove_24px"
                android:gravity="center"/>
        </layer-list>
    </item>

    <!-- Обычное состояние -->
    <item>
        <layer-list>
            <!-- Фон -->
            <item>
                <shape>
                    <solid android:color="@color/main_background"/>
                    <stroke android:width="1.5dp" android:color="@color/winbox_btn_border_up"/>
                    <corners android:radius="0.5dp"/>
                </shape>
            </item>
            <!-- Иконка -->
            <item android:drawable="@drawable/red_remove_24px"
                android:gravity="center"/>
        </layer-list>
    </item>
</selector>